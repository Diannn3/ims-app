# Source provenance

This cloud working mirror is pinned to GitHub repository `Diannn3/ims-app` at commit:

`a922d74f881d97075d61ac9277c6927efdabc21e`

A normal `git clone` was attempted first, but the execution container could not resolve `github.com`.
Current source files used for edits were therefore read from the authenticated GitHub connector at the pinned commit.
