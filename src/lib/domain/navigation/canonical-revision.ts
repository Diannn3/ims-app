/**
 * Stable label for the Git-owned map bundle. A release pipeline may replace
 * this with the source commit SHA; drafts must always record the label they
 * were created against before a rebase or approval can be attempted.
 */
export const CANONICAL_MAP_REVISION = 'map-v3-guided-wayfinding';
